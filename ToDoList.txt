* Change Update() parameter of the UI function.
* Change or add SetNameStruct() for nameFullString and
    nameAltString.
* Give comments in most of the files.
* Give comments in the CollectionFunction.
* Change the whole CollectionGlobal into static class. So it is not
    necessary anymore to call _collectionGlobalObject in every
    parameter.
* Change and check every indentation to spaces.
* Make a super class for most of pop up user interfaces objects.
* Make global variable for pop up object.
* Make bigger pop up objects and make them to adjust to the screen
    resolution accrodingly.
* Find a way to disable confirm button when the necessary input is
    not sufficient.
* Add exit callback to Exit Button. So when the exit button is
    pressed it closed this program.